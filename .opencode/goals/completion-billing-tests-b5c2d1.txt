Goal complete: Phase 5 billing semantics + Phase 6 regression tests.

Deliverables (all on main, pushed as 6982341):
- syncStripeSeats helper + calls in add/remove/role member routes; role route now recounts seats
- updateOrgSubscription column-map fix (status/tier wrote to nonexistent orgs columns — every webhook write failed)
- invoice handlers sync subscriptions-table status via updateSubscriptionStatus
- isOrgActive includes past_due (grace during Smart Retries), exported; AI gate comment updated
- priceIdToPlan maps real env IDs incl. pro; BILLING_PLANS + Env types extended
- CLI: test/setup.ts HOME isolation + vitest.config.ts; token-refresh.test.ts (5 tests)
- Fixed real bug found by new test: CliTokenProvider.refreshing never reset on rejection (poisoned singleton)
- Server: requireUser.test.ts (6), org-authz.test.ts (9), tier-gate.test.ts (12)

Evidence:
- pnpm -r test: core 28 + server 31 + cli 42 = 101 passed (was 69)
- server/cli tsc: 0 errors; web vite build: success
- seat sync + past_due policy present (grep verified); all 4 test files exist

Deferred (explicit): trial_will_end stays a refresh-only handler (no email infra);
resend path unchanged; Auth0 Native app + API-identifier check remain user dashboard actions.
